# Test Environment Variables
# DO NOT put real credentials here - this file may be committed to version control

# Database Configuration (Test Database)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/demand_letters_test
TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/demand_letters_test

# JWT Configuration
JWT_SECRET=test-secret-key-for-integration-tests-only
JWT_EXPIRES_IN=1h
REFRESH_TOKEN_SECRET=test-refresh-secret-key-for-integration-tests-only
REFRESH_TOKEN_EXPIRES_IN=7d

# AWS Configuration (Use LocalStack or mocks for testing)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test-access-key
AWS_SECRET_ACCESS_KEY=test-secret-key

# S3 Configuration (Mock or LocalStack)
S3_BUCKET_NAME=test-demand-letters-documents
S3_ENDPOINT=http://localhost:4566  # LocalStack endpoint

# Lambda Configuration (Mock)
AI_PROCESSOR_LAMBDA_ARN=arn:aws:lambda:us-east-1:000000000000:function:test-ai-processor

# Email Configuration (Mock)
EMAIL_FROM=noreply@test.com
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=test
SMTP_PASS=test

# Application Configuration
NODE_ENV=test
PORT=3001
API_URL=http://localhost:3001
FRONTEND_URL=http://localhost:5173

# Logging
LOG_LEVEL=error
TEST_LOGGING=false  # Set to 'true' to enable logging during tests

# Rate Limiting (disabled for tests)
RATE_LIMIT_ENABLED=false

# Redis (optional for tests)
REDIS_URL=redis://localhost:6379/1

# Bedrock (Mock for tests)
BEDROCK_MODEL_ID=anthropic.claude-v2
BEDROCK_REGION=us-east-1
